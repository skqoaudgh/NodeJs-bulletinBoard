<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>MAY Board</title>
        <link rel="stylesheet" type="text/css" href="/view/form_style.css">
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    </head>
    <body>
        <p id="get_error" style="display:none"><%= error %></p>
        <div class="login-page">
            <div class="form">
                <p id="error">
                    <%  if(error == 1) { %>
                        이미 사용중인 아이디입니다.
                    <% } else if(error == 2) { %>
                        이미 사용중인 닉네임입니다.
                    <% } else if(error == 3) { %>
                        입력한 아이디와 비밀번호가 일치하지 않습니다. 아이디 또는 비밀번호를 다시 한번 입력해 주세요. 
                    <% } %>
                </p>
                <form class="register-form" action="/regist" method="post" onsubmit="return Regcheck()">
                    <input type="text" id="reg_id" name="reg_id" placeholder="name"/>
                    <input type="text" id="reg_nick" name="reg_nick" placeholder="nickname"/>
                    <input type="password" id="reg_pwd" name="reg_pwd" placeholder="password"/>
                    <input type="text" id="reg_email" name="reg_email" placeholder="email address"/>
                    <button>create</button>
                    <p class="message">이미 가입하셨나요? <span>로그인</span></p>
                </form>
                <form class="login-form" action="/login" method="post">
                    <input type="text" id="login_id" name="login_id" placeholder="username"/>
                    <input type="password" id="login_pwd" name="login_pwd" placeholder="password"/>
                    <button>login</button>
                    <p class="message">아직 가입하지 않으셨나요? <span>회원가입</span></p>
                </form>
            </div>
        </div>
        <script type="text/javascript" src="/view/form_script.js"></script>
    </body>
</html>